Screenshots used by the top-level README.

Expected files:

| File | Screen | How to get there |
|---|---|---|
| `sessions.png` | Sessions list | `cd` into a project with history, run `ccss` |
| `start.png` | The start chooser | run `ccss` in a folder with history |
| `start-empty.png` | The start chooser, no history | run `ccss` in a folder Claude Code has never been used in |
| `transcript.png` | A conversation | press `enter` on a session |
| `delete.png` | Delete confirmation | press `d` on a session or project |

Capture at roughly 120x32 so the footer isn't shortened, and keep the same
terminal theme across all four so they look like one set.
